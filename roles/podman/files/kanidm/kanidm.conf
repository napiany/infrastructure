server {
    listen 443 ssl;
    listen 8443 ssl proxy_protocol;
    listen [::]:443 ssl;

    server_name idm.napiany.com;

    ssl_certificate /etc/letsencrypt/live/idm.napiany.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/idm.napiany.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/idm.napiany.com/chain.pem;

    include snippets/universal_paths.conf;
    include snippets/security.conf;
    include snippets/permissions.conf;
    include snippets/cross-origin-security.conf;
    include snippets/proxy.conf;
    include snippets/robots.conf;

    resolver 10.89.100.1 valid=30s ipv6=off;

    location / {
        set $upstream_kanidm https://kanidm:8443;
        proxy_ssl_name idm.napiany.com;
        proxy_ssl_verify on;
        proxy_pass $upstream_kanidm;
    }
}
