---
- name: 'Run gateway preflight checks'
  ansible.builtin.include_tasks: 'preflight.yaml'

- name: 'Configure the gateway package repository'
  ansible.builtin.include_tasks: 'repository.yaml'

- name: 'Install gateway packages'
  ansible.builtin.include_tasks: 'packages.yaml'

- name: 'Render gateway configuration'
  ansible.builtin.include_tasks: 'configuration.yaml'

- name: 'Apply pending gateway config restarts before starting services'
  ansible.builtin.meta: 'flush_handlers'

- name: 'Start gateway services'
  ansible.builtin.include_tasks: 'runtime.yaml'
